<div class="container">
	<div class="jumbotron mt-2">
		<h1 class="mt-0 mb-5 text-center">{{title}}</h1>
		<div class="row mx-4">
			{{#each data}}
			<div class="col-lg-4 col-md-4 col-sm-12 mb-4 mx-auto">
				<div class="card bg-dark text-light center">
					<a href="/products/{{this.id}}" class="text-light">
						<img class="card-img-top" src="{{this.pic}}">
						<div class="card-body">
								<h5 class="card-title text-center my-3">{{this.brand}} {{this.name}}</h5>
						</div>
					</a>
				</div>
			</div>
			{{/each}}
		</div>
	</div>
</div>
<!--
							<p>
								<ul> 
									{{#each data}}
										<li>{{this.name}} || {{this.type}} || {{this.description}} || {{this.brand}} || {{this.price}}</li>
									{{/each}}
								</ul>
							</p>
								<h7 class="card-title">Hobbies: </h7>
								<ul>
									{{#each hobbies}}
										<li>{{this}}</li>
									{{/each}}
								</ul>
		<div class="col-lg-8 col-md-8 mb-4 mx-auto">
			<div class="card bg-dark text-light">
				<div class="card-body">
					<p class="card-text">
						<ul> 
							{{#each data}}
								<li>{{this.name}} || {{this.type}} || {{this.description}} || {{this.brand}} || {{this.price}}</li>
							{{/each}}
						</ul>
					</p>
				</div>
			</div>
		</div>
-->
	
