<div class="container">
  <div class="jumbotron mt-2">
    <h3 align="center">Top 10 Customers with most number of orders</h3>
      <div class="col-lg-8 col-md-8 col-sm-8 mb-2 mx-auto">
        <div class="card bg-secondary text-light center">
          <table style="width:80%" class="table table-sm mx-auto">
            <tr>
              <th>Name</th>
              <th>Email</th>
              <th>Number of Orders</th>
            </tr>
            {{#each topTenCustomersWithMostOrders}}
            <tr>
              <td>
                {{!-- <a href="/admin/customers/{{this.id}}">
                </a> --}}
                {{this.first_name}} {{this.last_name}}
              </td>
              <td>
                {{this.email}}
              </td>
              <td>
                {{this.number_of_orders}}
              </td>
            </tr>
           {{/each}}
          </table>
        </div>
      </div>
    <h3 align="center">Top 10 with highest payment</h3>
    {{#each data}}
      <div class="col-lg-8 col-md-8 col-sm-8 mb-2 mx-auto">
        <div class="card bg-secondary text-light center">
          <table style="width:80%" class="table table-sm mx-auto">
            <tr>
              <th>Name</th>
              <th>Number of Orders</th>
            </tr>
            {{#each topTenCustomersWithMostOrders}}
            <tr>
              <td>
                {{!-- <a href="/admin/customers/{{this.id}}">
                </a> --}}
                  {{this.first_name}} {{this.last_name}}
              </td>
              <td>
                {{this.number_of_orders}}
              </td>
            </tr>
           {{/each}}
          </table>
        </div>
      </div>
    {{/each}}

  </div>
</div>